연산 전에 두 피연산자의 타입이 다를 경우 타입을 일치시킨다.

double > float > long > int > short > byte
- 두 피연산자의 타입을 같게 일치시킨다. (둘중에 저장공간 크기가 더 큰 타입으로 일치)
ex) 피연산자의 타입이 'int' 보다 작은 'short' 타입이면 'int'로 변환
- 변수를 여러개 연산했을때 결과값은 피연산자 중 표현 범위가 가장 큰 변수 타입을 가지게 된다.